extends Node2D
#class_name Board 
#static instance: Board
#
#func _ready():
	#Event.powerup_spawn_requested.connect(spawn_powerup)
	#for i in INF:
		#await get_tree().create_timer(5).timeout
		#prints(i*5)
#

#func spawn_powerup(pos:Vector2, type:Powerup.Type):
